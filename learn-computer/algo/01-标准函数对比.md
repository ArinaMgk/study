
### 字符串操作

sprintf=outsfmtbuf  (char* buf, const char* fmt, ...)->int;


vsprintf=outsfmtlstbuf  (char* buf, const char* fmt, para_list lst)->int;

**Size-fixed String Formatting**
- stdlib `sprintfn(addr, size, fmt, ...)`
- unicpp `String(addr, size).Format(fmt, ...)`

