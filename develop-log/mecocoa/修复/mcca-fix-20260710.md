现在 ring_coreid 能同时实现“syscall 期间不得迁移”和“调度筛选 affinity”。我觉得没有问题。继续拆也没有要拆的。

PickNext() 把很多 ready 线程筛掉，是因为ready 线程必须在别的cpu上恢复吧？

还有一件事：现在每个cpu都有一个临时栈。每个ring>0程序在 syscall 时都会经过临时栈，然后迅速切换为自己的内核栈。返回用户态也需要用临时栈。现在的一个保证是：只要程序的栈指针离开了临时栈的时候左右，临时栈里面的数据就可以发生变化，被其他程序使用。

---

我现在的理解：
1、或许可以通过去掉 CF_TRANS_FRAME 这个字段、在返回时重新定位当前cpu 临时栈解决问题
2、你说 owner CPU 没有及时 take responsibility 我不理解。因为正常情况下有定时的 Schedule IPI 信号（注意不是 WakeIPI），不至于现在一直卡死。而且现在好像新增了一个 cpu 核可以去过期就绪队列里找线程，不至于没有过期队列被某个AP卡死的情况吧。我不太确定，你可以再去检查检查

周期性 reschedule IPI 的来源:Handint_PIT->WakeOneIdleApForReadyWork->SendRescheduleIPI

｛
短期补锅路
保留 CF_TRANS_FRAME 模型不动，继续要求 same-core continuation。
那就得把“pinned ready thread 如何定向催 owner CPU schedule”做强，不能只靠 WakeOneIdleApForReadyWork() 这种轮询式 helper。
结构性正路
把 CF_TRANS_FRAME / IF_TRANS_FRAME 拿掉，返回时重建当前 CPU 临时栈。
这样 continuation 可迁移，ring_coreid 就不必再承担这么硬的正确性职责。
｝

---

1. 不可能一直卡住，还是内核
2. Handint_PIT->WakeOneIdleApForReadyWork->SendRescheduleIPI
3. 不可能一直卡住，还是内核

---

```
【REST】有限休息，不会一直卡住的，所以忽视睡死的可能，不需要被打扰

【ring_coreid】我刚刚把这个字段删掉了
1、请问我可以删掉吗？不会影响什么吗？
2、删掉后仍然卡死，看起来与这个无关

...


【其他提示】
1、 单核没有问题，多核有！
2、可能与某些数据被破坏有关
3、发生在 Shell 创建/关闭过程，越频繁越容易发生
4、REST TMSG IPI just_schedule  这几个机制都没有问题   忽略
```




