

```
【20260423】
我计划使用 read / write 操作一个 vtty 设备。现在输入数据，停止输入的条件如下：输入字符达到数量，或者遇到换行符。用户输入退格符号的时候会正确处理，去掉相关字符，用户调用read时输入可以回显
现在D:\her\COTLAB\src\cotlab.cpp sysread 之后，现在直接卡住了。输入不回显，回车也没有动静。请检查是什么原因
相关文件：syscall.cpp, filesys.cpp
---

批量读取时：回显由驱动层的 readfl 负责。
单字符阻塞读取时：回显由系统调用层的 sysc_READ 负责。
```


